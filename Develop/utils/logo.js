class Logo{
    constructor(color, shape, text){
        this.color = color;
        this.shape = shape;
        this.text = text;
    }
    generateLogo(){
        //generates logo and creates SVG file in output folder
    }
}

module.exports = Logo;
